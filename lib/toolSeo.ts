export const SITE_NAME = "ConvertGeine";

export function freeToolTitle(tool: string) {
  return `Free ${tool} Online | ${SITE_NAME}`;
}

export function freeToolDescription(tool: string) {
  return `Use our free online ${tool.toLowerCase()} tool. Fast, secure, mobile-friendly, and no registration required.`;
}
